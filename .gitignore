*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build is intentionally NOT ignored — Hostinger can't run `npm`,
# so the compiled Vite assets must be committed and deployed via git.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# AI assistant / internal dev artifacts — keep out of the client deliverable.
# (Matches names/paths, NOT file contents.)
/.claude/
/.codex/
CLAUDE.md
AGENTS.md
GEMINI.md
.mcp.json
.cursorrules
/build.zip
/public/build.zip

# Internal developer documentation — not part of the client deliverable
/docs/
